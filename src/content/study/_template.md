---
title: 제목은 검색 결과에 그대로 뜬다. 무엇을 따라 해 보는지 드러나게
description: 목록과 검색 결과에 보이는 두 줄. 이 글을 따라 하면 무엇을 보게 되는지 쓴다.
date: 2026-01-01
tags: [d365, sales]
# 목록에 쓸 썸네일. 안 적으면 본문 첫 캡처를 자동으로 쓴다
# thumbnail: /captures/파일명.png
# 데모 영상이 있으면 (없으면 줄째 지운다)
# video: https://www.youtube.com/watch?v=영상ID
# 화면 템플릿. 스터디의 기본은 페이지 넘김(딕) — ## 절 하나가 한 페이지가 되고
# 클릭·방향키로 넘긴다. 스크롤 글로 쓰고 싶으면 아래 줄을 살린다
# template: note
---

<!--
  스터디 글 쓰는 법. 이 파일은 밑줄(_)로 시작해서 빌드에서 제외된다.

  1. 이 파일을 복사 → src/content/study/영문-슬러그.md 로 저장
  2. 캡처는 붙여넣으면 끝 — Win+Shift+S 로 찍고 글 안에서 Ctrl+V.
     파일은 public/captures/<글이름>/ 에 자동 저장되고 링크도 삽입된다.
     (경로 정리는 빌드가 알아서 한다. 파일을 손으로 넣어도 물론 된다)
  3. 아래 형태를 반복한다

     ## 소제목
     ![](/captures/파일명.png)
     1. 첫 번째 설명
     2. 두 번째 설명

  이 세 덩어리가 연달아 나오면 화면에서 자동으로 2단이 된다.
  왼쪽에 소제목과 설명, 오른쪽에 캡처가 놓인다.

  번호 목록은 화면에서 ① ② ③ 으로 나온다. 캡처 이미지에 같은 번호의
  원문자를 그려 넣으면 설명과 화면이 짝이 맞는다.

  목록 번호를 5부터 시작하면(5. 6. 7.) ⑤ ⑥ ⑦ 로 나온다.

  캡처가 하나도 없는 글은 배포 빌드에서 실패한다. 스터디 글은 화면이 본체다.
-->

여는 문단. 무엇을 왜 따라 해 보는지 한 문단으로. 어떤 환경에서 확인했는지도 여기 적는다.

## 첫 화면에서 확인할 것

![](/captures/sample.svg)

1. 화면에서 첫 번째로 볼 지점
2. 두 번째로 볼 지점
3. 세 번째로 볼 지점

캡처 사이에 이런 설명 문단을 자유롭게 끼워 넣어도 된다. 2단으로 바뀌는 건
"소제목 + 이미지 + 번호 목록"이 붙어 있을 때뿐이다.

## 다음 화면

![](/captures/sample.svg)

1. 여기서 막히면 무엇을 먼저 보는지
2. 흔히 놓치는 설정

## 확인하지 못한 것

- 검증 범위 밖으로 남겨 둔 것. 이 항목이 있는 글이 더 믿을 만하다.
